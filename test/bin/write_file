#!/bin/sh

set -e

output=$1
shift

if test x = "x$TEST_VERBOSE_OUTPUT"
then
  verbose_echo() {
    :
  }
else
  verbose_echo() {
    echo "$*"
  }
fi

verbose_echo writing to $output.
echo "$@" >> $output
verbose_echo wrote to $output.
